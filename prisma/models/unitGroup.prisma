model UnitGroup {
    idUnitGroup Int    @id @default(autoincrement()) @db.Integer
    name        String @unique @db.VarChar(20) // např. "hmotnost", "objem", "česnek"

    unitGroupUnit UnitGroupUnit[]

    @@schema("unit")
}
