/**
 * navbarSharedConfig - Shared configuration for navbar styles
 * Defines common glassmorphism styles to ensure consistency between Desktop and Mobile
 */
export const navbarSharedConfig = {
  // Glassmorphism effect (background + blur)
  glass: "backdrop-blur-xl bg-background/80",

  // Border color for glass effect
  borderColor: "border-white/20 dark:border-white/10",
};
