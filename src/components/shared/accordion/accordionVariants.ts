import { tv } from '@heroui/theme';

const accordionVariants = tv({
  base: [
    "flex",
    "flex-col",
    "p-0",
    "m-0",
    "pt-[0.9375rem]",
    "list-none",
    "list-image-[initial]",
    "w-full",
  ],
});

export default accordionVariants;
