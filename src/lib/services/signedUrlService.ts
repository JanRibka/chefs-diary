// import RouteParamsType from "../types/common/RouteParamsType";

export default function fromRoute() {
// routeName: string,
// routeParams: RouteParamsType,
// expirationDate: Date
  // const expiration = Math.floor(expirationDate.getTime() / 1000); // timestamp
  // const baseUrl = process.env.APP_URL?.replace(/\/$/, "") || "";
  // const routePath = buildRoutePath(routeName, routeParams);
  // const url = new URL(baseUrl + )
}
